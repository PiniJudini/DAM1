let numero = prompt("Introduce un número para saber si es divisible por 3: ")
if(numero%3==0)
    document.writeln("El número " + numero + " es divisible por 3")
else
    document.writeln("El número " + numero + " no es divisible por 3")